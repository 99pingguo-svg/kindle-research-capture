# Privacy and publishing rules

Captured books, screenshots, search indexes, session logs, ASINs, account
identifiers, local paths, and user-specific metadata are private outputs.

Do not commit them.

This repository contains only generic tooling and synthetic documentation.
The default .gitignore blocks common capture outputs.

Before publishing changes, run a secret/path scan and inspect git diff.
